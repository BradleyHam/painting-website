import React from 'react'
import classes from './Hero.module.scss'

const Hero = () => {
    return (

        <div className={classes.hero}>
            <div className={classes.block}>
                <h1>Increase the value to your Property</h1>
            </div>
        </div>
    );
}

export default Hero;